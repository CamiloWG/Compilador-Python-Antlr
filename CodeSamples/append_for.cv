numeros = [];

for i in range(0, 100) {
    numeros.append(i + 1);
}

def esPar(numero) {
    if(numero % 2 == 0) {
        return 1;
    } else {
        return 0;
    }
}


for num in numeros {
    if(esPar(num)) {
        print(num);
    }
}