numero = input("Ingrese un número:");

def facto(num) {
    val = 1;
    num = num + 1;
    for i in range(1, num) {
        val = val * i;
    }
    return val;
}

res = facto(numero);

print("El factorial de ", numero," es: ", res);